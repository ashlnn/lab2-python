# Користувач вводить слово
s = input("Введіть слово: ")

# Отримуємо половину літер слова від початку до кінця
if len(s) >= 2:
    half_length = len(s) // 2
    half_string = s[:half_length]
    print(f"Початковий рядок: '{s}'")
    print(f"Половина рядка: '{half_string}'")
else:
    print(f"Рядок '{s}' занадто короткий для отримання половини")
